import {TableCard} from "../tableCard/TableCard"
import {TableCardColiving} from "../tableCard/TableCardColiving"
import {TableCardDestacados} from "../tableCard/TableCardDestacados"

export const Cards = () => {
    
    return (
        <div>
        <TableCard/>
        <TableCardColiving/>
        <TableCardDestacados/>
        </div>
    )
}